# 이 파일을 복사해서 .env 로 만들고 값을 채우세요:  cp .env.example .env
# Vite 는 VITE_ 접두사가 붙은 변수만 클라이언트에 노출합니다.

# API 서버 주소. 공통 접두사 /api 를 포함합니다 (apis/*.js 의 경로는 /api 를 생략).
VITE_API_BASE_URL=http://localhost:8080/api